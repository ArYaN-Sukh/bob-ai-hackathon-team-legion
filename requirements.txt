# Vercel installs Python dependencies from the repository root.  Keep this
# manifest explicit: the current Vercel uv installer does not support -r
# include directives in requirements.txt.
fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.52
psycopg[binary]==3.2.10
pydantic==2.13.5
pydantic-settings==2.3.0
httpx==0.27.0
pytest==8.2.0
pytest-asyncio==0.23.7
python-dotenv==1.0.1
